{
	"name": "backtrail",
	"version": "0.1.0",
	"description": "Backtrail",
	"engines": {
		"node": ">=24.14",
		"pnpm": ">=11"
	},
	"files": [
		"README.md"
	],
	"scripts": {
		"typecheck": "pnpm -r --if-present typecheck",
		"build": "pnpm -r --if-present build",
		"test": "pnpm -r --if-present test",
		"deps:update": "taze -r --include-locked --maturity-period 2"
	},
	"devDependencies": {
		"taze": "catalog:",
		"typescript": "catalog:"
	}
}