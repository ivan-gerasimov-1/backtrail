{
	"name": "backtrail",
	"version": "0.1.0",
	"description": "Backtrail",
	"engines": {
		"node": ">=24.14",
		"pnpm": ">=11"
	},
	"files": [
		"README.md"
	],
	"scripts": {
		"typecheck": "pnpm -r --if-present typecheck",
		"build": "pnpm -r --if-present build",
		"test": "pnpm -r --if-present test",
		"deprecated:deps:update": "ncu --workspaces --upgrade --enginesNode --format group --reject @types/node",
		"deps:update": "taze -r --include-locked --maturity-period 2"
	},
	"devDependencies": {
		"npm-check-updates": "22.2.0",
		"taze": "catalog:",
		"typescript": "catalog:"
	}
}